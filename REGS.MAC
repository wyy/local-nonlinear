! OUTPUT NODES INFO
! REGS,ARG1

*GET,Nnod,NODE,0,COUNT
*DEL,Xy,,NOPR
*DIM,Xy,ARRAY,NNOD,3
*DEL,Nodesl,,NOPR
*DIM,Nodesl,ARRAY,NNOD
*GET,Nd,NODE,0,NUM,MIN
*DO,I,1,Nnod,1
  Nodesl(I) = Nd
  Xy(I,1) = NX(Nd)
  Xy(I,2) = NY(Nd)
  Xy(I,3) = NZ(Nd)
  Nd = NDNEXT(Nd)
*ENDDO

*MWRITE,Nodesl,NODES_%ARG1%,TXT,,IJK
(F10.0)
*MWRITE,Xy,NODESXY_%ARG1%,TXT,,IJK
(E25.15)
